server:
  port: ${PORT:8190}
  servlet:
    context-path: /api


spring:
  config:
    additional-location: ${SPRING_CONFIG_ADDITIONAL_LOCATION}
  jpa:
    open-in-view: false
    generate-ddl: true
    hibernate:
      ddl-auto: update
      show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:mysql://localhost:3306/landside
    username: user
    password: password
  logging:
    level:
      org.hibernate.tool.schema: DEBUG
      org.hibernate.SQL: DEBUG
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: mypassword

